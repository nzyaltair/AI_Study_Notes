# 监督微调（Supervised Fine-Tuning, SFT）

## 1. 概述

### 1.1 定义与目标

监督微调（Supervised Fine-Tuning, SFT）是大语言模型（LLM）训练流水线中的关键阶段，通过在高质量的指令-响应对数据集上训练预训练模型，使模型能够理解并执行人类指令。

%% 核心目标：将通用预训练模型调整为能够遵循特定指令、生成符合人类预期输出的模型 %% ^core-objective

### 1.2 在LLM训练流水线中的位置

```
预训练（Pretraining） → 监督微调（SFT） → 对齐（Alignment） → 部署（Deployment）
```

- **预训练**：从海量文本中学习通用语言能力和知识
- **SFT**：学习遵循人类指令，生成符合预期的响应
- **对齐**：进一步调整模型行为，使其符合人类价值观和偏好（如RLHF、DPO）
- **部署**：将模型部署到实际应用场景

### 1.3 SFT与其他范式的关系

| 范式 | 核心特点 | 适用场景 | 与SFT的关系 |
|------|----------|----------|-------------|
| 零样本推理 | 无需示例，直接执行指令 | 通用任务 | SFT的基础，SFT可提升零样本性能 |
| 少样本推理 | 提供少量示例，执行指令 | 复杂任务 | SFT可减少对少样本示例的依赖 |
| 上下文学习 | 在上下文提供示例，执行指令 | 特定任务 | SFT可增强上下文学习能力 |
| 指令微调 | 在指令-响应对上训练 | 通用指令遵循 | 与SFT同义或高度相关 |
| RLHF | 基于人类反馈的强化学习 | 对齐人类偏好 | 通常在SFT之后进行 |

## 2. 典型SFT流程

### 2.1 数据构建

#### 2.1.1 数据类型

- **指令-响应对**：最常见的SFT数据格式，包含指令和期望输出
- **多轮对话**：包含多轮交互的对话历史和响应
- **结构化任务**：如分类、摘要、翻译等特定任务数据

#### 2.1.2 高质量SFT数据集构建原则

1. **多样性**：
   - 覆盖多种任务类型（生成、分类、摘要、翻译等）
   - 包含不同难度级别
   - 涵盖多种领域和主题
   - 支持多语言

2. **高质量**：
   - 指令清晰明确
   - 响应准确、完整、有用
   - 语法正确，表达流畅
   - 避免偏见和有害内容

3. **避免标签泄露**：
   - 确保测试数据未出现在训练集中
   - 避免指令中包含答案信息
   - 防止数据污染

4. **难度梯度**：
   - 从简单任务到复杂任务
   - 从具体指令到抽象指令
   - 从短文本到长文本

5. **真实场景**：
   - 反映实际用户需求
   - 包含真实对话场景
   - 涵盖边缘情况

#### 2.1.3 主流SFT数据集

| 数据集名称 | 规模 | 类型 | 特点 |
|------------|------|------|------|
| Alpaca | 52K | 指令-响应对 | 基于Self-Instruct生成 |
| Dolly | 15K | 指令-响应对 | 人工生成，开放商用 |
| ShareGPT | 90K+ | 多轮对话 | 从ShareGPT网站收集 |
| WizardLM | 70K | 指令-响应对 | 经过进化的高质量指令 |
| UltraChat | 1.5M | 多轮对话 | 大规模多轮对话数据 |
| OIG | 40M | 混合 | 多种指令数据混合 |

### 2.2 数据格式设计

#### 2.2.1 常见格式

##### Alpaca格式
```json
{
  "instruction": "解释量子计算的基本原理",
  "input": "",
  "output": "量子计算利用量子力学现象（如叠加态和纠缠）来处理信息..."
}
```

##### ChatML格式
```
<|im_start|>system
你是一个 helpful 的AI助手<|im_end|>
<|im_start|>user
解释量子计算的基本原理<|im_end|>
<|im_start|>assistant
量子计算利用量子力学现象（如叠加态和纠缠）来处理信息...<|im_end|>
```

##### 多轮对话格式
```json
{
  "conversations": [
    {
      "from": "human",
      "value": "你好"
    },
    {
      "from": "assistant",
      "value": "你好！有什么可以帮助你的吗？"
    },
    {
      "from": "human",
      "value": "解释量子计算"
    },
    {
      "from": "assistant",
      "value": "量子计算利用量子力学现象..."
    }
  ]
}
```

#### 2.2.2 格式设计原则

- **清晰的角色区分**：明确区分用户、助手、系统角色
- **明确的分隔符**：使用特殊标记分隔不同部分
- **一致性**：保持格式在整个数据集上的一致性
- **灵活性**：支持单轮和多轮对话
- **可扩展性**：便于后续扩展和修改

### 2.3 损失函数

#### 2.3.1 标准交叉熵损失

SFT通常使用标准交叉熵损失函数，仅计算模型对目标响应的预测损失，忽略输入部分。

```python
# 简化的SFT损失计算
loss = 0
for i in range(output_length):
    loss += -log_softmax(model_output[input_length + i])[target[i]]
loss = loss / output_length
```

#### 2.3.2 特殊处理

- **仅计算响应部分损失**：输入部分不计算损失
- **标签平滑**：减少模型的过度自信，提高泛化能力
- **长度归一化**：对不同长度的输出进行归一化

### 2.4 训练策略

#### 2.4.1 全参数微调

- **定义**：微调模型的所有参数
- **优点**：
  - 充分利用数据，性能潜力高
  - 适合小模型或高质量数据集
- **缺点**：
  - 计算资源需求高
  - 内存消耗大
  - 训练时间长

#### 2.4.2 部分参数微调

- **LoRA（Low-Rank Adaptation）**：
  - 在模型层之间插入低秩矩阵
  - 仅训练低秩矩阵参数
  - 内存效率高，训练速度快

- **QLoRA**：
  - 结合LoRA和量化技术
  - 可在单个GPU上微调大型模型
  - 支持4位量化

- **Adapter**：
  - 在模型层之间插入小型适配器模块
  - 仅训练适配器参数
  - 结构灵活

- **Prefix Tuning**：
  - 优化输入前缀，不修改模型权重
  - 适合生成任务
  - 内存效率高

## 3. 工程实践

### 3.1 主流框架与工具

#### 3.1.1 Hugging Face Transformers

- **核心优势**：丰富的预训练模型库、易用的API、完善的文档
- **SFT支持**：
  - `Trainer`类支持SFT训练
  - `DataCollatorForSeq2Seq`用于处理序列到序列数据
  - `AutoModelForCausalLM`支持因果语言模型

#### 3.1.2 DeepSpeed

- **核心优势**：分布式训练支持、内存优化、ZeRO优化
- **SFT支持**：
  - 支持大规模模型SFT
  - 自动混合精度训练
  - 梯度累积

#### 3.1.3 Unsloth

- **核心优势**：高速训练、内存高效、易于使用
- **SFT支持**：
  - 优化的LoRA实现
  - 支持快速SFT
  - 低内存消耗

### 3.2 训练代码结构

#### 3.2.1 基本训练流程

```python
from transformers import Trainer, TrainingArguments, AutoModelForCausalLM, AutoTokenizer
from datasets import load_dataset

# 1. 加载模型和分词器
model = AutoModelForCausalLM.from_pretrained("meta-llama/Llama-2-7b-hf")
tokenizer = AutoTokenizer.from_pretrained("meta-llama/Llama-2-7b-hf")

# 2. 加载和处理数据集
dataset = load_dataset("tatsu-lab/alpaca")

def preprocess_function(examples):
    # 格式化指令-响应对
    inputs = [f"Instruction: {instruction}\nInput: {input}\nResponse:" for instruction, input in zip(examples["instruction"], examples["input"])]
    targets = examples["output"]
    # 编码
    model_inputs = tokenizer(inputs, max_length=512, truncation=True, padding="max_length")
    labels = tokenizer(targets, max_length=512, truncation=True, padding="max_length")
    # 设置标签，忽略输入部分
    labels["input_ids"] = [label + [-100]*(512 - len(label)) for label in labels["input_ids"]]
    return model_inputs

processed_dataset = dataset.map(preprocess_function, batched=True)

# 3. 配置训练参数
training_args = TrainingArguments(
    output_dir="./results",
    per_device_train_batch_size=8,
    gradient_accumulation_steps=4,
    learning_rate=2e-5,
    lr_scheduler_type="cosine",
    warmup_ratio=0.1,
    num_train_epochs=3,
    logging_steps=100,
    save_steps=500,
    fp16=True,
)

# 4. 初始化Trainer
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=processed_dataset["train"],
    eval_dataset=processed_dataset["test"] if "test" in processed_dataset else None,
)

# 5. 开始训练
trainer.train()
```

#### 3.2.2 LoRA训练示例

```python
from peft import LoraConfig, get_peft_model

# 配置LoRA
lora_config = LoraConfig(
    r=16,  # LoRA秩
    lora_alpha=32,  # LoRA缩放因子
    target_modules=["q_proj", "v_proj"],  # 应用LoRA的模块
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM"
)

# 应用LoRA
model = get_peft_model(model, lora_config)

# 训练过程与全参数微调类似
```

### 3.3 关键超参数选择

#### 3.3.1 学习率

- **初始学习率**：通常在1e-5到5e-5之间
- **调度策略**：
  - 余弦退火（常用）
  - 线性衰减
  - 多项式衰减
- **Warmup**：
  - 通常使用0.05-0.1的warmup比例
  - 帮助模型适应新的训练数据

#### 3.3.2 Batch Size

- **per_device_train_batch_size**：根据GPU内存调整，通常8-32
- **gradient_accumulation_steps**：用于模拟更大的batch size
- **总batch size**：per_device_train_batch_size × gradient_accumulation_steps × num_gpus
- **推荐范围**：256-1024（根据模型大小调整）

#### 3.3.3 序列截断策略

- **max_seq_length**：
  - 通常设置为预训练模型的最大序列长度
  - 或根据硬件条件调整
- **截断方式**：
  - 从左截断（保留响应部分）
  - 从右截断（保留输入部分）
  - 智能截断（根据重要性）

#### 3.3.4 过拟合防控

- **dropout**：在模型层中添加dropout，通常0.05-0.1
- **weight decay**：权重衰减，通常0.01
- **early stopping**：监控验证损失，提前停止训练
- **regularization**：如L1/L2正则化
- **数据增强**：对训练数据进行变换

## 4. 典型陷阱与应对方案

### 4.1 数据污染

- **问题**：训练数据包含测试数据或敏感信息
- **原因**：数据来源未验证、去重不彻底
- **应对方案**：
  - 严格分离训练/验证/测试集
  - 对数据进行全面去重
  - 验证数据来源
  - 进行数据审计

### 4.2 指令泛化不足

- **问题**：模型只能处理训练过的指令类型，泛化能力差
- **原因**：训练数据多样性不足、指令模板单一
- **应对方案**：
  - 增加训练数据多样性
  - 使用多种指令模板
  - 引入对抗样本
  - 进行指令改写

### 4.3 灾难性遗忘

- **问题**：SFT导致模型忘记预训练阶段学到的知识
- **原因**：训练数据分布与预训练数据差异大、训练步数过多
- **应对方案**：
  - 使用较小的学习率
  - 限制训练步数
  - 采用部分参数微调
  - 引入预训练数据的混合训练

### 4.4 响应质量不稳定

- **问题**：模型生成的响应质量波动大
- **原因**：训练数据质量不一致、损失函数设计不当
- **应对方案**：
  - 提高训练数据质量
  - 使用更复杂的损失函数
  - 增加数据多样性
  - 进行后处理

### 4.5 计算资源不足

- **问题**：无法训练大型模型
- **原因**：GPU内存不足、计算资源有限
- **应对方案**：
  - 采用部分参数微调（LoRA、QLoRA）
  - 使用更低精度（FP16、BF16）
  - 增加梯度累积步数
  - 使用分布式训练

## 5. SFT的局限性与未来发展

### 5.1 SFT的局限性

1. **数据依赖**：需要大量高质量的指令-响应对
2. **泛化能力**：对未见过的指令类型泛化能力有限
3. **对齐问题**：无法保证模型行为符合人类价值观
4. **成本高昂**：高质量数据标注成本高
5. **灾难性遗忘**：可能忘记预训练知识

### 5.2 与后续对齐技术的协同关系

```
SFT → DPO → RLAIF
```

- **SFT**：学习遵循指令，生成符合预期的响应
- **DPO（Direct Preference Optimization）**：基于人类偏好直接优化模型，无需RLHF的复杂流程
- **RLAIF（RL from AI Feedback）**：使用AI反馈替代人类反馈，降低对齐成本

### 5.3 新兴方向

#### 5.3.1 合成数据SFT

- **LLM-as-a-Data-Generator**：使用已有模型生成SFT数据
- **Self-Instruct**：让模型生成自己的训练数据
- **Evol-Instruct**：迭代优化生成的指令
- **优点**：降低数据标注成本，提高数据多样性

#### 5.3.2 课程学习微调

- **核心思想**：从简单任务逐步过渡到复杂任务
- **实现方式**：
  1. 按难度分级组织训练数据
  2. 先训练简单数据，再训练复杂数据
  3. 逐步提高训练难度
- **优点**：提高训练效率，改善模型性能

#### 5.3.3 多任务SFT

- **核心思想**：在单个模型中支持多种任务
- **实现方式**：
  1. 统一数据格式
  2. 使用任务类型标记
  3. 联合训练多种任务
- **优点**：提高模型通用性，降低部署成本

#### 5.3.4 领域自适应微调

- **核心思想**：针对特定领域优化模型
- **实现方式**：
  1. 收集领域特定数据
  2. 在领域数据上进行SFT
  3. 结合领域知识
- **优点**：提高模型在特定领域的性能

## 6. 前沿研究与进展

### 6.1 2024–2026年代表性工作

#### 6.1.1 合成数据生成

- **Self-Instruct (2024改进版)**：
  - 引入多轮对话生成
  - 提高数据多样性
  - 支持多种任务类型

- **Evol-Instruct v2**：
  - 更高效的指令进化算法
  - 生成更复杂的指令
  - 提高模型性能

#### 6.1.2 高效SFT方法

- **QLoRA 2.0**：
  - 支持2位量化
  - 进一步降低内存消耗
  - 提高训练速度

- **LoRA++**：
  - 改进的LoRA结构
  - 更高的参数效率
  - 更好的性能

#### 6.1.3 多模态SFT

- **Multimodal SFT**：
  - 支持文本、图像、音频等多模态输入
  - 统一的指令格式
  - 跨模态理解和生成

- **Vision-Language SFT**：
  - 结合视觉和语言指令
  - 支持图像描述、视觉问答等任务

#### 6.1.4 对齐技术融合

- **SFT + DPO联合优化**：
  - 同时优化指令遵循和人类偏好
  - 简化训练流程
  - 提高模型性能

- **Iterative SFT-Align**：
  - 迭代进行SFT和对齐
  - 逐步提高模型质量
  - 适应动态变化的需求

## 7. 最佳实践与建议

### 7.1 数据方面

1. **优先保证数据质量**：宁可少而精，不要多而杂
2. **注重数据多样性**：覆盖多种任务、领域和难度
3. **使用多种数据来源**：结合人工标注、模型生成和公开数据集
4. **进行数据清洗和去重**：确保数据质量和唯一性
5. **验证数据分布**：确保数据分布与目标任务一致

### 7.2 模型选择方面

1. **根据任务选择合适的预训练模型**：
   - 通用任务：选择大规模模型
   - 特定领域：选择领域相关模型
   - 资源有限：选择小型模型或使用部分参数微调

2. **考虑模型架构**：
   - Decoder-only：适合生成任务
   - Encoder-Decoder：适合序列到序列任务
   - 多模态模型：适合多模态任务

### 7.3 训练方面

1. **从部分参数微调开始**：
   - 优先尝试LoRA或QLoRA
   - 根据需要过渡到全参数微调

2. **使用合适的训练框架**：
   - 小型模型：Hugging Face Transformers
   - 大型模型：DeepSpeed + Transformers
   - 快速实验：Unsloth

3. **监控训练过程**：
   - 跟踪训练损失和验证损失
   - 监控模型生成的样例
   - 检测过拟合迹象

4. **进行充分的评估**：
   - 使用多种评估指标
   - 进行人工评估
   - 测试在实际场景中的表现

## 8. 案例研究

### 8.1 LLaMA 2 SFT

- **数据规模**：约1M指令-响应对
- **训练策略**：全参数微调
- **关键技术**：
  - 高质量数据筛选
  - 多轮对话训练
  - 混合精度训练
- **结果**：模型能够理解并执行多种指令，性能显著提升

### 8.2 Mistral SFT

- **数据规模**：约800K指令-响应对
- **训练策略**：LoRA微调
- **关键技术**：
  - 高效数据生成
  - 课程学习
  - 低秩适应
- **结果**：在单GPU上高效完成训练，性能接近全参数微调

### 8.3 Qwen SFT

- **数据规模**：约2M指令-响应对
- **训练策略**：全参数微调
- **关键技术**：
  - 多语言数据
  - 结构化任务训练
  - 高质量标注
- **结果**：模型支持多种语言，在中文任务上表现出色

## 9. 待办事项与未来工作

- [ ] 补充医疗领域SFT案例
- [ ] 添加更多部分参数微调方法的详细比较
- [ ] 完善多模态SFT内容
- [ ] 更新2026年最新研究成果
- [ ] 添加更详细的评估指标和方法
- [ ] 补充SFT与其他微调方法的对比
- [ ] 添加实际部署建议

## 关联内容

- 返回[[../大模型训练与对齐|大模型训练与对齐]] - 大模型训练与对齐主页面
- 参考[[../预训练流程/预训练流程|预训练流程]] - 预训练流程详细内容
- 参考[[../数据收集与清洗/数据收集与清洗|数据收集与清洗]] - 数据处理详细内容
- 参考[[../参数高效微调/参数高效微调|参数高效微调]] - 参数高效微调详细内容
- 参考[[../../5.大语言模型核心/注意力机制/注意力机制|注意力机制]] - 注意力机制详细内容

---

*本指南基于2026年1月的最新研究编写，内容将持续更新。*