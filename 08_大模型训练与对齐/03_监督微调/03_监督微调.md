# 监督微调

## 背景与发展

监督微调（Supervised Fine-Tuning, SFT）是大语言模型训练流水线中连接预训练与偏好对齐的关键阶段。InstructGPT 首先确立了 SFT 在对齐流程中的地位，Alpaca 证明了 Self-Instruct 生成的数据可以有效微调开源模型，LLaMA 2/3、Qwen 等模型均将 SFT 作为指令跟随能力的主要来源。

## 核心思想

通过在高质量指令-回答对数据上训练预训练模型，使模型从"续写文本"转变为"理解并执行人类指令"。SFT 的核心原则是数据质量远比数量重要——少量高质量数据的训练效果优于大量低质量数据。

## 技术原理

### 损失函数

SFT 使用标准交叉熵损失，但仅计算模型对目标响应部分的预测损失，输入部分（指令）不参与损失计算：

$$L = -\frac{1}{|y|} \sum_{t=1}^{|y|} \log P(y_t \mid x, y_{<t}; \theta)$$

其中 $x$ 为输入指令，$y$ 为目标响应。输入部分的标签设为 -100（PyTorch 中被忽略）。

### 数据格式

**Alpaca 格式**：`instruction` + `input` → `output`

**ChatML 格式**：使用 `<|im_start|>` / `<|im_end|>` 分隔符区分 system/user/assistant 角色

**多轮对话格式**：包含多轮交互的对话历史

### 数据集构建原则

1. **多样性**：覆盖多种任务类型（生成、分类、摘要、翻译）、难度级别和领域
2. **高质量**：指令清晰、响应准确完整、语法正确
3. **难度梯度**：从简单到复杂、从具体到抽象
4. **真实场景**：反映实际用户需求，包含边缘情况

### 主流数据集

| 数据集 | 规模 | 特点 |
|-------|------|------|
| Alpaca | 52K | Self-Instruct 生成 |
| ShareGPT | 90K+ | 真实多轮对话 |
| UltraChat | 1.5M | 大规模多轮对话 |
| Dolly | 15K | 人工生成 |

### 训练策略

**全参数微调**：微调所有参数，性能潜力高，适合小模型或高质量数据集。计算资源需求大，存在灾难性遗忘风险。

**参数高效微调**：仅训练少量参数（[[LoRA]]/[[QLoRA]]/[[Adapter]]），内存效率高，训练速度快，适合大模型。

### 关键超参数

| 参数 | 常见范围 | 说明 |
|------|---------|------|
| 学习率 | 1e-5 ~ 5e-5 | 低于预训练学习率 |
| Warmup 比例 | 0.05 ~ 0.1 | 防止训练初期不稳定 |
| Batch Size | 56-1024 | 含梯度累积的总批次 |
| 序列长度 | 1024-32768 | 根据任务和硬件调整 |
| 训练轮次 | 3-10 | 通常 3-5 轮即可 |
| Weight Decay | 0.01 | 防止过拟合 |

### 灾难性遗忘的应对

- 使用较小学习率
- 限制训练步数
- 采用参数高效微调（冻结大部分参数）
- 在训练数据中混入少量预训练数据

## 发展演进

InstructGPT (2022，SFT+RLHF) → Alpaca (2023，Self-Instruct 数据) → LLaMA 2 (2023，百万级 SFT 数据) → Qwen (2024，多语言 SFT) → 合成数据驱动 SFT

## 关键算法·模型

- **Self-Instruct**：让模型生成自己的训练数据
- **Evol-Instruct**：迭代优化指令复杂度
- **LoRA 微调**：低秩适配器，仅训练 ~1% 参数
- **QLoRA**：4-bit 量化 + LoRA，单卡微调 70B 模型

## 应用场景

- **指令跟随**：让模型理解并执行各类指令
- **领域适配**：医疗、法律、金融等垂直领域微调
- **多语言能力**：通过多语言 SFT 数据增强跨语言能力
- **多模态扩展**：结合视觉/音频指令的多模态 SFT

## 与其他技术关系

- SFT 是 [[预训练流程]] 之后的必要阶段
- SFT 模型作为 [[RLHF]] / [[DPO]] 的初始策略
- 参数高效微调方法（[[LoRA]]、[[QLoRA]]）是 SFT 的主流实现
- SFT 数据质量依赖 [[数据收集与清洗]] 的方法论
